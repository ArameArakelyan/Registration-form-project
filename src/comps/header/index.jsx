import React from "react";
import { useState } from "react";


function Header(params) {

    return(
        <header>
            <input type="text" />
            <button>Add</button>
        </header>
    )
}

export default Header